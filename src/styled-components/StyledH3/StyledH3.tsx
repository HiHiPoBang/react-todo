import styled from 'styled-components';
import { H3_FONT_SIZE, REGULAR_FONT_COLOR } from '../commonVariable';

const StyledH3 = styled.h2`
  margin: 5px 0px;
  font-size: ${H3_FONT_SIZE};
  font-weight: 300;
  color: ${REGULAR_FONT_COLOR};
`;

export default StyledH3;
