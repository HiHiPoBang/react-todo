import styled from 'styled-components';
import { H2_FONT_SIZE, REGULAR_FONT_COLOR } from '../commonVariable';

const StyledH2 = styled.h2`
  margin: 5px 0px;
  font-size: ${H2_FONT_SIZE};
  font-weight: 300;
  color: ${REGULAR_FONT_COLOR};
`;

export default StyledH2;
